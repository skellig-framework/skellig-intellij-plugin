name ( request ) {
    f4 = ${key : ${dd : ${fff_21 : 44}}}

    // inner parameter here is not a string, but its value will be one
    // so it will be highlighted as a parameter key
    a = ${_'${b}'_ }

    b = ${a "bb"d\'d }
    f = ${a${b}:'${311:12}'}
    sd = '1' "gg\"\'# g \'" \"\"\' 21
}